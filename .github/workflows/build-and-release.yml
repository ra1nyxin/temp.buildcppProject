name: OS-ImGui Expert Synthesis v2.1

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build_dll:
    runs-on: windows-2022
    permissions:
      contents: write

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup MSVC Toolchain
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Advanced Dependency Structuring
        shell: powershell
        run: |
          # 1. 创建符合 #include "imgui/xxx.h" 要求的物理结构
          New-Item -ItemType Directory -Path "imgui" -Force
          
          # 2. 拉取 ImGui 并填充到 imgui 文件夹
          git clone --depth 1 https://github.com/ocornut/imgui.git imgui_repo
          Copy-Item "imgui_repo/*.h" "imgui/" -Force
          Copy-Item "imgui_repo/*.cpp" "imgui/" -Force
          # 同时拷贝 Backends，OS-ImGui 可能需要它们
          Copy-Item "imgui_repo/backends/imgui_impl_win32.*" "imgui/" -Force
          Copy-Item "imgui_repo/backends/imgui_impl_dx11.*" "imgui/" -Force
          
          # 3. 拉取 OS-ImGui 并提取核心文件到根目录
          git clone --depth 1 https://github.com/TKazer/OS-ImGui.git osimgui_repo
          Get-ChildItem -Path "osimgui_repo/OS-ImGui" -Include *.h, *.cpp -Recurse | Copy-Item -Destination "." -Force
          
          Write-Host "[SUCCESS] Directory structure synthesized."

      - name: Technical Synthesis (CL Compilation)
        shell: cmd
        run: |
          :: /I. -> 寻找根目录下的 OS-ImGui.h
          :: /I./imgui -> 辅助寻找，但主要是为了让 "imgui/imgui.h" 路径生效
          cl.exe /nologo /O2 /MT /W3 /std:c++17 /I. /DUNICODE /D_UNICODE /DOSIMGUI_INTERNAL ^
          main.cpp ^
          imgui/imgui.cpp imgui/imgui_draw.cpp imgui/imgui_tables.cpp imgui/imgui_widgets.cpp ^
          imgui/imgui_impl_win32.cpp imgui/imgui_impl_dx11.cpp ^
          OS-ImGui.cpp OS-ImGui_External.cpp ^
          /link /DLL /OUT:Hex_Internal_Module.dll ^
          user32.lib gdi32.lib d3d11.lib d3dcompiler.lib dwmapi.lib

      - name: Binary Integrity Verification
        shell: powershell
        run: |
          if (Test-Path "Hex_Internal_Module.dll") {
              $hash = Get-FileHash "Hex_Internal_Module.dll" -Algorithm SHA256
              Write-Host "[FINISHER] SHA256: $($hash.Hash)"
          } else {
              Write-Error "DLL Generation Failed."
              exit 1
          }

      - name: Archive Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Hex_Internal_Module
          path: Hex_Internal_Module.dll
